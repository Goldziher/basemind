# yaml-language-server: $schema=https://goreleaser.com/static/schema.json

version: 2

project_name: basemind

before:
  hooks:
    - cargo install --locked cargo-zigbuild
    - cargo fetch --locked

builds:
  - builder: rust
    flags:
      - --release
    targets:
      - x86_64-unknown-linux-gnu
      - aarch64-unknown-linux-gnu
      - x86_64-apple-darwin
      - aarch64-apple-darwin
      - x86_64-pc-windows-gnu

archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}-{{ .Target }}"
    format_overrides:
      - goos: windows
        formats: [zip]

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

release:
  # keep-existing lets a re-dispatch / re-run reconcile against an already-created
  # release instead of failing — e.g. publishing the Homebrew formula on a re-run
  # when the binaries already uploaded on the first pass.
  mode: keep-existing
  footer: >-

    ---

    Released by [GoReleaser](https://github.com/goreleaser/goreleaser).

brews:
  - name: basemind
    description: "Full AI context layer for coding agents — code-map, RAG, memory, web, git"
    homepage: "https://github.com/Goldziher/basemind"
    license: "MIT"
    repository:
      owner: Goldziher
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
      branch: main
    directory: Formula
    commit_author:
      name: goreleaserbot
      email: bot@goreleaser.com
    commit_msg_template: "Brew formula update for {{ .ProjectName }} version {{ .Tag }}"
    skip_upload: false
